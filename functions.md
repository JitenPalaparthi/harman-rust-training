

- fn --> funtion pointer, the usage is limited, cannot use all sorts of varialbes, as long as the functions owns the vaiable, it is fine .


- Fn -> it is a. closure/anonymous function, that can have access to the global variabels in a immutable way
- FnMut -> mutable global variables 
- FnOnce -> Transfer the ownership insidd the function
